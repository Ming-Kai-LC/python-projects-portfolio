# Windows Automation & Scripting Project Dependencies

# Core Jupyter dependencies
jupyter>=1.0.0
notebook>=6.5.4
ipykernel>=6.25.0

# For syntax highlighting and code formatting
ipython>=8.12.0

# For working with PowerShell in Jupyter (optional but recommended)
# Note: Requires separate installation of PowerShell kernel
# Run: dotnet tool install --global PowerShell
# Then: pwsh -Command "Install-Module -Name PowerShellKernel -Force"
# Then: pwsh -Command "Install-PowerShellKernel"

# Useful for demonstrations and examples
pywin32>=305          # Windows API access from Python
psutil>=5.9.5         # System and process utilities
colorama>=0.4.6       # Colored terminal output
tabulate>=0.9.0       # Pretty table printing
